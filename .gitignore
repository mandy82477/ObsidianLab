# Secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/

# Logs (keep locally, don't commit)
logs/

# 例外：task_scheduler.log 是 pipeline 的執行紀錄，雲端 routine 每次執行與中止都
# 必須把它 commit + push——容器銷毀後「保留在本機」沒有意義，那是唯一能事後追查
# 「那天到底跑了沒、為什麼中止」的證據。
#
# 上面的 `logs/` 沒有前導斜線，會比對任意深度的 logs 目錄，因此也擋住了
# CLAUDE_NEWS/src/logs/。git 規定「父目錄被排除時無法單獨 re-include 子檔案」，
# 所以要先把該目錄放行、再排除目錄內容、最後單獨放行這一個檔。
# （2026-07-25 雲端執行被此規則擋下，當次改用 git add -f 繞過後回報登記。）
!CLAUDE_NEWS/src/logs/
CLAUDE_NEWS/src/logs/*
!CLAUDE_NEWS/src/logs/task_scheduler.log

# OS
Thumbs.db
desktop.ini
.DS_Store

# IDE
.vscode/
.idea/

# Claude Code worktrees
.claude/worktrees/




# Ignore Smart Environment folder
.smart-env

# Obsidian UI 狀態：Obsidian 開著就會持續改寫，永遠處於「已修改但不該 commit」，
# 每次推送撞車 rebase 都得為它 stash 一次——2026-08-09 就這樣把同批 stash 裡的
# community-plugins.json（dataview 啟用狀態）一起弄丟。設定類（app / appearance /
# core-plugins / community-plugins）與外掛本體仍追蹤，只排除這兩個純 UI 檔。
.obsidian/workspace.json
.obsidian/graph.json