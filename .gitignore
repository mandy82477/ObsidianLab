# Secrets
.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/

# Logs (keep locally, don't commit)
logs/

# CLAUDE_NEWS 於 2026-09-12 分割為獨立 repo mandy82477/claude-news。
# 本機資料夾原地不動（仍嵌在 vault 內），但 vault 不再追蹤它——它自己有 .git。
# 原本為 CLAUDE_NEWS/src/logs/task_scheduler.log 開的 re-include 例外一併移到該 repo 的 .gitignore。
CLAUDE_NEWS/

# OS
Thumbs.db
desktop.ini
.DS_Store

# IDE
.vscode/
.idea/

# Claude Code worktrees
.claude/worktrees/




# Ignore Smart Environment folder
.smart-env

# Obsidian UI 狀態：Obsidian 開著就會持續改寫，永遠處於「已修改但不該 commit」，
# 每次推送撞車 rebase 都得為它 stash 一次——2026-08-09 就這樣把同批 stash 裡的
# community-plugins.json（dataview 啟用狀態）一起弄丟。設定類（app / appearance /
# core-plugins / community-plugins）與外掛本體仍追蹤，只排除這兩個純 UI 檔。
.obsidian/workspace.json
.obsidian/graph.json

# Claude Code 個人設定（權限允許清單，含本機絕對路徑）——每台機器自己的，不進 repo（2026-09-04 公開前掃描）
**/.claude/settings.local.json
